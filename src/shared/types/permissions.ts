/*
import * as permissions from '../../shared/const/permission.ts';

const permissionsValues = [...Object.values(permissions)] as const;

export type Permission = (typeof permissionsValues)[number];
*/
